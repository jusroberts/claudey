import Config

config :wigglebot_server,
  llama_url: "http://100.x.x.x:8080",
  google_maps_api_key: "your-key-here"

# Firebase Cloud Messaging (push notifications to the phone).
# Service account JSON from Firebase console → Project settings →
# Service accounts → Generate new private key.
# config :wigglebot_server, :fcm_service_account_path, "/path/to/service-account.json"

# Actual Budget via the actual-http-api sidecar container
# (https://github.com/jhonderson/actual-http-api).
# config :wigglebot_server, :actual_api,
#   url: "http://127.0.0.1:5007",
#   api_key: "your-self-chosen-wrapper-key",
#   sync_id: "your-budget-sync-id"
